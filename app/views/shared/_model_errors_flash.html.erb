<% if model.errors.any? %>
  <div class="red col-span-full">
    <h2><%= pluralize(model.errors.count, "error") %> prohibited this survey from being saved:</h2>

    <ul>
      <% model.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
